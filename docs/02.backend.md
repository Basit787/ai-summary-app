# Backend Architecture

## Overview

The backend is responsible for:

- Receiving uploaded documents from the frontend
- Validating uploaded files
- Uploading documents to Amazon S3
- Storing document metadata and the S3 URL in the database
- Triggering the AI Service asynchronously
- Returning the `documentId` to the frontend

---

# Architecture Diagram

```mermaid
flowchart LR

    FE[Frontend]
    V[File Validation]
    API[Backend API]
    S3[(Amazon S3)]
    DB[(Database)]
    AI[AI Service]

    FE -->|Upload Document| V

    V -->|Invalid| FE
    V -->|Valid| API

    API -->|Upload File| S3
    S3 -->|Return S3 URL| API

    API -->|Store Metadata + S3 URL| DB

    API -->|Trigger AI Job| AI

    API -->|Return documentId| FE
```

---

# Sequence Diagram

```mermaid
sequenceDiagram
    autonumber

    participant Frontend
    participant Backend
    participant Amazon S3
    participant Database
    participant AI Service

    Frontend->>Backend: Upload Document

    Backend->>Backend: Validate File

    alt Invalid File
        Backend-->>Frontend: Validation Error
    else Valid File
        Backend->>Amazon S3: Upload File
        Amazon S3-->>Backend: Return S3 URL

        Backend->>Database: Save Metadata + S3 URL

        Backend->>AI Service: Trigger Async Job

        Backend-->>Frontend: documentId (processing)
    end
```

---

# Backend Flow

```text
Frontend
    │
    ▼
Upload Request
    │
    ▼
Validate File
    │
    ├── Invalid
    │      │
    │      └── Return Validation Error
    │
    └── Valid
           │
           ▼
      Upload to Amazon S3
           │
           ▼
      Receive S3 URL
           │
           ▼
Store Metadata + S3 URL
           │
           ▼
Trigger AI Service
           │
           ▼
Return documentId
           │
           ▼
Frontend
```
